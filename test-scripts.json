{
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:run": "vitest run",
    "test:watch": "vitest watch",
    "test:performance": "vitest run --reporter=verbose src/test/performance",
    "test:a11y": "vitest run src/test/accessibility",
    "test:features": "vitest run src/test/features",
    "test:e2e": "playwright test",
    "analyze:bundle": "npm run build && npx bundle-analyzer dist/assets/*.js",
    "analyze:performance": "node scripts/performance-audit.js"
  }
}